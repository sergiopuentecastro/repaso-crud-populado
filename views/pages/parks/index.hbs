<div class="container">
    <h2 style="font-weight: 100">Todos los parques</h2>
    <div class="row">
        {{#each parks}}
        <div class="col-sm-6">
            <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
                <p class="card-text">{{description}}</p>
            </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>