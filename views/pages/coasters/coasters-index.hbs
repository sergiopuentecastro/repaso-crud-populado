<div class="container">
  <h1>Montañas rusas</h1>
  <hr>
  <div class="row">
    {{#each coasters}}
    <div class="col-sm-6">
      
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>
          <p class="card-text">{{description}}</p>
          <small><strong>Longitud: </strong> {{length}}m | <strong>Inversiones: </strong> {{inversions}} | <strong>Parque: </strong> {{park_id.name}}</small>
          <br>
          <a href="coasters/{{id}}" class="btn btn-sm btn-warning">Ver detalles</a>
        </div>
      </div>
      
    </div>
    {{/each}}
  </div>
</div>
